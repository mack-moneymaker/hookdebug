<% content_for(:title) { "Free Webhook Tester & API Debugger" } %>
<% content_for(:description) { "Test webhooks online for free. Generate unique webhook URLs instantly, inspect HTTP requests in real-time, replay webhooks, and debug your API integrations." } %>

<div class="max-w-4xl mx-auto px-4 py-16 text-center">
  <h1 class="text-5xl font-extrabold text-white mb-4">
    Test Webhooks <span class="text-emerald-400">Instantly</span>
  </h1>
  <p class="text-xl text-gray-400 mb-8 max-w-2xl mx-auto">
    Generate a unique URL, send any HTTP request to it, and inspect the payload in real-time.
    No signup required. Free webhook tester for developers.
  </p>

  <%= form_with url: endpoints_path, method: :post, class: "flex justify-center gap-3 mb-6" do |f| %>
    <%= f.text_field :name, placeholder: "Optional name (e.g., Stripe test)", class: "bg-gray-800 border border-gray-700 text-white rounded-lg px-4 py-3 w-80 focus:outline-none focus:ring-2 focus:ring-emerald-500" %>
    <%= f.submit "Generate Webhook URL →", class: "bg-emerald-600 hover:bg-emerald-500 text-white font-semibold px-6 py-3 rounded-lg cursor-pointer transition" %>
  <% end %>

  <p class="text-gray-500 text-sm">Free temporary URLs expire in 48 hours. <a href="/signup" class="text-emerald-400 hover:underline">Sign up</a> for permanent URLs.</p>
</div>

<div class="max-w-6xl mx-auto px-4 pb-20">
  <div class="grid md:grid-cols-3 gap-8">
    <div class="bg-gray-900 border border-gray-800 rounded-xl p-6">
      <div class="text-3xl mb-3">⚡</div>
      <h3 class="text-lg font-semibold text-white mb-2">Real-Time Inspection</h3>
      <p class="text-gray-400 text-sm">See incoming webhooks appear instantly via WebSocket. Headers, body, query params — everything displayed live.</p>
    </div>
    <div class="bg-gray-900 border border-gray-800 rounded-xl p-6">
      <div class="text-3xl mb-3">🔄</div>
      <h3 class="text-lg font-semibold text-white mb-2">Replay Requests</h3>
      <p class="text-gray-400 text-sm">Capture a webhook and replay it to any URL. Perfect for debugging and testing your endpoint's response.</p>
    </div>
    <div class="bg-gray-900 border border-gray-800 rounded-xl p-6">
      <div class="text-3xl mb-3">🎛️</div>
      <h3 class="text-lg font-semibold text-white mb-2">Custom Responses</h3>
      <p class="text-gray-400 text-sm">Configure what status code, headers, and body your webhook URL returns. Simulate any server behavior.</p>
    </div>
    <div class="bg-gray-900 border border-gray-800 rounded-xl p-6">
      <div class="text-3xl mb-3">🔍</div>
      <h3 class="text-lg font-semibold text-white mb-2">Search & Filter</h3>
      <p class="text-gray-400 text-sm">Find specific requests in your history. Filter by method, search headers and body content.</p>
    </div>
    <div class="bg-gray-900 border border-gray-800 rounded-xl p-6">
      <div class="text-3xl mb-3">👥</div>
      <h3 class="text-lg font-semibold text-white mb-2">Team Sharing</h3>
      <p class="text-gray-400 text-sm">Share webhook URLs and their history with your team. Debug together in real-time.</p>
    </div>
    <div class="bg-gray-900 border border-gray-800 rounded-xl p-6">
      <div class="text-3xl mb-3">🔐</div>
      <h3 class="text-lg font-semibold text-white mb-2">Permanent URLs</h3>
      <p class="text-gray-400 text-sm">Sign up to get permanent webhook URLs that never expire. Keep your integration testing stable.</p>
    </div>
  </div>
</div>

<div class="bg-gray-900 border-t border-gray-800 py-16">
  <div class="max-w-4xl mx-auto px-4 text-center">
    <h2 class="text-3xl font-bold text-white mb-4">How It Works</h2>
    <div class="grid md:grid-cols-3 gap-8 mt-8">
      <div>
        <div class="text-4xl font-bold text-emerald-400 mb-2">1</div>
        <h3 class="text-white font-semibold mb-1">Generate a URL</h3>
        <p class="text-gray-400 text-sm">Click the button above to get a unique webhook URL instantly.</p>
      </div>
      <div>
        <div class="text-4xl font-bold text-emerald-400 mb-2">2</div>
        <h3 class="text-white font-semibold mb-1">Send Requests</h3>
        <p class="text-gray-400 text-sm">Point your webhooks, APIs, or curl commands at your URL.</p>
      </div>
      <div>
        <div class="text-4xl font-bold text-emerald-400 mb-2">3</div>
        <h3 class="text-white font-semibold mb-1">Inspect & Debug</h3>
        <p class="text-gray-400 text-sm">See every request in real-time. Headers, body, everything.</p>
      </div>
    </div>
  </div>
</div>
